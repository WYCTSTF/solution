#include <bits/stdc++.h>
using namespace std;
int main() {
  int tt;
  cin >> tt;
  while (tt--) {
    int n;
    vector<string> v(n);
    map<int, vector<string>> mp;
    for (int i = 0; i < n; ++i) {
      cin >> v[i];
    }
  }
  return 0;
}